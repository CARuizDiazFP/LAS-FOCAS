# Nombre de archivo: requirements.txt
# Ubicación de archivo: requirements.txt
# Descripción: Dependencias del proyecto

fastapi==0.112.2  # Framework web para APIs (unificado)
uvicorn[standard]==0.30.6  # Servidor ASGI para FastAPI (unificado)
httpx==0.27.0  # Cliente HTTP asíncrono
sqlalchemy==2.0.32  # ORM para interactuar con la base de datos
psycopg[binary]==3.1.19  # Driver PostgreSQL en formato binario
orjson==3.10.6  # Serializador JSON de alto rendimiento (unificado)
jinja2==3.1.6  # Motor de plantillas para UI
python-multipart==0.0.9  # Soporte de formularios para FastAPI
bcrypt==4.1.2  # Hash de contraseñas (uso directo sin Passlib)
itsdangerous==2.2.0  # Firmas para sesiones (SessionMiddleware)
pydantic==2.9.2  # Modelos y validación (usado en schemas de módulos)
pydantic-settings==2.4.0  # Configuración basada en BaseSettings (PYD v2)
pandas==2.2.2  # Procesamiento de datos para informes
openpyxl==3.1.2  # Lectura de Excel para pandas
python-docx==0.8.11  # Generación de documentos DOCX para informes

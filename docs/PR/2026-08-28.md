# Nombre de archivo: 2026-08-28.md
# Ubicación de archivo: docs/PR/2026-08-28.md
# Descripción: PR diario del 2026-08-28

# PR Diario - 2026-08-28

## Resumen de Cambios

Trabajo grande pusheado a `dev` en el día: **submódulo ODFs** (Cromo Red, clase 69) más
**"ODFs asociadas" en Detalle de Servicio** (tracking, sistema separado), ejecutado con
`superpowers:subagent-driven-development` (9 tareas + 1 diagnóstico real previo + 1 ronda de fix en
la revisión final de rama completa, 11 commits propios en el rango `1a2d254..0d58648`, excluyendo un
commit ajeno de otra sesión — `99a2306`, "fix(baneos)" — que aterrizó en `dev` en el medio). Diseño
completo en `docs/decisiones.md` (entrada 2026-08-28) y `docs/modulo_ingesta_cromo.md` (submódulo
ODFs). El ticket original pedía todo sobre Cromo; un diagnóstico real contra Cromo (30 objetos de
clase 69, corrido dentro de `lasfocasdev-cromo-worker`) reveló que dos de los tres pedidos en
realidad pertenecen a un sistema completamente distinto (`core/parsers/tracking_parser.py`), ya
existente y funcionando — ver la entrada de decisiones para el detalle completo.

### Parte A — Submódulo `/infra/odfs` (Cromo Red, clase 69)

1. **Modelo + migración** (`20260828_01_cromo_odfs`): tabla `cromo_odfs` nueva (no reusa
   `cromo_botellas`), `tipo_elemento` (ODF/EMPALME/SIN_CLASIFICAR) con CHECK en vez de enum nativo.
2. **Parser puro**: `parse_odf`/`Odf`, `clasificar_tipo_elemento_odf` (regex finalizado contra datos
   reales — el patrón `O-`/`Patch`/`F-`/`Empalme` del ticket no aplica a Cromo, pertenece a
   tracking). Registrado en el dispatcher del parser.
3. **Fase de ingesta**: `fase_odfs` (directa, `show=["SHOW","REL_ATTRIBUTE","TIME"]` para exponer
   `tp[]`) + `continuar_corrida(modo="SOLO_ODF"|"COMPLETA")`. `COMPLETA` incluye ODFs
   incondicionalmente desde ahora.
4. **Endpoints de sólo lectura**: `odf_inventario.py`, `odf_detalle.py`, `verificador.py::servicios_por_odf`
   + 3 rutas en `web/app/main.py`, mismo patrón que Cables.
5. **Frontend**: `InventarioOdfsCromoView.vue` (`/infra/cromo/odfs`, filtros Nodo/Cliente), 
   `OdfDetalleCromoView.vue`, entrada "ODFs" en `AppShell.vue`.
6. **Cierre de cobertura**: test de cancelación cooperativa para `fase_odfs` (huella real que faltaba).

### Parte B — ODFs asociadas en Detalle de Servicio (tracking, sin tocar Cromo)

7. **Endpoint nuevo** `GET /api/infra/servicios/{servicio_id}/odfs` en `web/app/main.py`, sobre
   `tracking_parser.py` ya existente (sin modificarlo). Nunca confía en `Empalme.es_transito`
   persistido (verificado real: sólo se setea en una de varias rutas de resolución) — siempre
   re-deriva en vivo. Enriquecimiento de cámara en batch, no N+1.
8. **Frontend**: sección "ODFs asociadas" en `ServicioDetalleView.vue`, agrupada por ruta, toggle
   para mostrar empalmes no-tránsito.
9. **Cierre Parte B**: 32 tests nuevos para `tracking_parser.py` (0% de cobertura antes de esta
   tarea, ahora 93%), checklist de QA manual contra 5 servicios reales.

### Revisión final de rama completa (opus) — 1 ronda de fix

- **I1 (regresión real):** registrar clase 69 en el dispatcher rompió silenciosamente
  `validador_datos_service.py` (Tool Kit "Validar datos DB Cromo") — mostraba una card en blanco sin
  explicación en vez del error claro anterior. Corregido con una rama `isinstance(dominio, Odf)`.
- **I2 (dato falso + patrón rechazado):** el filtro `servicio` de `odf_inventario.py` usaba `EXISTS`
  correlacionado justificado por un comentario que subestimaba el volumen real (7.955, no
  "decenas/centenas"). Reescrito al patrón `IN` no correlacionado ya documentado en `inventario.py`.
- **M1:** docstring de `fase_conteo` corregido (ya incluye ODF).

### Selector "Sólo ODFs" + primera ingesta real verificada (mismo día, post-revisión)

- **Decisión de producto:** sí exponer `SOLO_ODF` en la UI. Selector "Alcance de la corrida"
  agregado a `AdminIngestaCromo.vue`, deshabilita/ignora la grilla de clases de botella en ese modo.
- **Primera corrida real** (`SOLO_ODF`, `psize=20, max_paginas=1`, dentro de
  `lasfocasdev-cromo-worker`, previa reconstrucción de esa imagen — estaba desactualizada respecto a
  `dev`): 20 objetos reales creados, 0 errores. 17/20 con cables asociados, 3/20 sin ninguno (mismo
  ~10% que ya vio el diagnóstico de la Tarea 0).
- **Checklist de verificación real pedido explícitamente por el usuario**, confirmado end-to-end
  contra los 3 endpoints reales (no sólo la tabla): ODFs se guardan (`app.cromo_odfs`, 20 filas),
  cables se relacionan (`cables_asociados` con nombres reales), servicios se asocian
  (`servicios_por_odf` resolvió YPF SOCIEDAD ANONIMA vía 2 cables reales), y las "ODFs asociadas" de
  Detalle de Servicio (sistema de tracking, independiente de esta ingesta) siguen clasificando
  correctamente (servicio 91719 real).
- **Hallazgo operativo, no un bug:** varios objetos de clase 69 del barrido directo carecen de
  `n_id` propio — cae en el fallback ya existente y compartido por todos los tipos de objeto
  (`parser._resolver_n_id`), no algo nuevo ni específico de ODF.

## Comandos Ejecutados

```bash
pytest tests/ -q                              # 1195 passed, 20 skipped, 4 fallos preexistentes no
                                               # relacionados (falta web/frontend/dist/index.html)
alembic upgrade head                          # 20260828_01_cromo_odfs aplicada contra lasfocasdev-postgres
docker cp scripts/cromo_sonda.py lasfocasdev-cromo-worker:/tmp/  # diagnóstico real, sólo lectura
docker exec lasfocasdev-cromo-worker python -c "... ejecutar_sonda() ..."  # 30 objetos reales de clase 69
docker compose -f deploy/docker-compose.dev.yml --env-file .env.dev build cromo_worker web
docker exec lasfocasdev-cromo-worker python /tmp/run_solo_odf.py  # ejecutar_ingesta(modo="SOLO_ODF"), 20 creadas, 0 errores
```

## Impacto y Riesgos

- **Migración pura de tabla nueva**, sin impacto en tablas existentes — head único confirmado
  (`20260828_01` sobre `20260825_02`), `downgrade()` implementado y probado.
- **`app.cromo_odfs` ya tiene datos reales en dev** (20 filas de la primera corrida `SOLO_ODF`
  verificada) — la feature está activa y verificada end-to-end, no sólo testeada con mocks.
- Prod (`lasfocas-*`) no fue tocado — todo el trabajo corrió contra `lasfocasdev-*` y el stack real
  de Cromo vía `lasfocasdev-cromo-worker` (sólo lectura contra Cromo, escritura sólo en la DB dev
  propia).

# Nombre de archivo: 2026-01-07.md
# Ubicación de archivo: docs/PR/2026-01-07.md
# Descripción: Registro diario de cambios del 2026-01-07

## Resumen de cambios
- Se crea la carpeta `Keys/` en la raíz para alojar llaves locales fuera del control de versiones.
- Se agrega la ruta `Keys/` al `.gitignore` para excluir cualquier contenido sensible.
- Se actualiza la documentación viva (`docs/Mate_y_Ruta.md`) para reflejar el nuevo directorio ignorado y la fecha de hoy.

## Contexto y alcance
- Solicitud orientada a ordenar el almacenamiento de llaves/artefactos locales y evitar que se suban al repositorio.
- Se toca únicamente configuración de ignore y documentación; no se modifican servicios ni código de ejecución.

## Cambios realizados
- `.gitignore`: se añade `Keys/` bajo la sección de entornos y secretos.
- `docs/Mate_y_Ruta.md`: se actualiza la fecha de última actualización y se registra la nueva carpeta ignorada.
- Se crea la carpeta `Keys/` en la raíz del proyecto (solo para uso local, no versionada).

## Tareas realizadas y pendientes
- [x] Añadir carpeta `Keys/` y excluirla del repo.
- [x] Documentar el cambio en PR diario y en Mate y Ruta.
- [ ] Validar que no existan llaves o credenciales fuera del directorio ignorado.
- [ ] Revisar y consolidar los cambios previos pendientes en rama `sla` antes de un release.

## Criterios de aceptación y validación
- `Keys/` debe existir en la raíz del proyecto y estar listada en `.gitignore`.
- No se deben versionar archivos ubicados dentro de `Keys/`.
- La documentación debe reflejar la existencia de la carpeta y la fecha de actualización.
- No se ejecutaron pruebas automatizadas (cambio de infraestructura mínima).

## Impacto en seguridad y datos
- Mejora el manejo de llaves locales evitando su exposición en git.
- Sin cambios en esquemas de datos ni superficies de red.

## Compatibilidad y migraciones
- Sin impacto en compatibilidad ni migraciones de base de datos.

## Evidencia de validación manual
- Verificación manual de `.gitignore` y presencia de la carpeta `Keys/` en la raíz.

## Próximos pasos
- Validar que cualquier artefacto sensible existente se traslade a `Keys/` o a secretos gestionados (Docker Secrets / vault interno).

# Nombre de archivo: 2026-08-06.md
# Ubicación de archivo: docs/PR/2026-08-06.md
# Descripción: PR diario del 2026-08-06 — Etapas 3 a 6 de ingesta Cromo (servicio real, API/SSE, interfaz y verificador)

# PR Diario - 2026-08-06

## Resumen de Cambios

Etapa 3 del módulo de ingesta Cromo: servicio real (`core/services/cromo/ingesta.py`) que lee paginado
desde Cromo, clasifica por `vmax`, escribe en `app.cromo_*` con transacción por página, y audita cada
corrida. Validado con una corrida real acotada (`max_paginas=1`) contra el Cromo de producción de
Metrotel y `lasfocasdev-postgres`, que encontró y permitió corregir tres bugs reales antes de comitear.

**Etapa 4 (mismo día)**: endpoints admin en `web/app/main.py` para disparar una corrida, seguirla en
vivo por SSE, ver histórico/detalle y cancelarla. Primer uso de `AsyncSessionLocal` y de una tarea en
background (`asyncio.create_task`) en ese archivo — decisión de arquitectura tomada con el usuario
(todo en el proceso de `web/`, sin proxear a la app `api/` como hacen las otras ingestas largas).

**Etapa 5 (mismo día)**: vista `/admin/ingesta/cromo` en el panel admin — selector de clases, `psize`
fijo a los 5 valores permitidos, progreso en vivo consumiendo el SSE de la Etapa 4 con `EventSource`
nativo del browser (primer uso en el proyecto), histórico y detalle de corridas. De paso, un bug real
en `enmascarar()` (capítulo 6.3 del diseño): sin tope, un JWT de OAuth2 real de Cromo (~2000
caracteres) producía una línea de log con ~2000 asteriscos.

**Etapa 6 (mismo día) — cierra el roadmap del módulo**: verificador de servicios — tres consultas de
sólo lectura (`core/services/cromo/verificador.py`) sobre las tablas ya pobladas por la Etapa 3: qué
servicios pasan por un cable entero, por un tubo/buffer, o por los cables que tienen una botella como
extremo. Vista `/infra/cromo/verificador` en el panel operativo (no `/admin`: es consulta, no
administración). Al validar contra datos reales de la Etapa 3 apareció un caso real de referencia
colgada que obligó a rediseñar el criterio de "objeto no encontrado" antes de comitear (detalle abajo).

## Contexto y Alcance

- **Módulos afectados**: `core/services/cromo/ingesta.py` (nuevo, luego extendido para Etapa 4),
  `db/models/cromo.py` (fix puntual de esquema del enum), `web/app/main.py` (Etapa 4, nuevos endpoints),
  `web/frontend/src/api/cromo.ts` y `web/frontend/src/admin/views/AdminIngestaCromo.vue` (nuevos, Etapa
  5), `core/services/cromo/config.py` (fix de `enmascarar()`, Etapa 5),
  `core/services/cromo/verificador.py`, `web/frontend/src/views/VerificadorCromoView.vue` (nuevos,
  Etapa 6), `web/frontend/src/components/app-shell/AppShell.vue` (nueva entrada de navegación, Etapa 6).
- **Objetivo**: cerrar la Etapa 3 del roadmap (`docs/modulo_ingesta_cromo.md`), habilitada tras la Etapa
  2 (persistencia) y las decisiones de producto del usuario sobre la clase 69 (ODF) y `psize`; y, el
  mismo día, las Etapas 4 (API + progreso en vivo), 5 (interfaz) y 6 (verificador) — cierra el roadmap
  completo del módulo.

## Cambios Realizados

- `core/services/cromo/ingesta.py`: FASE 1 (conteo vía `stats[].count`), FASE 2 (cables, barrido
  directo), FASE 3 (botellas + árbol completo: fusiones, cables embebidos, tubos, pelos), FASE 4
  (reconciliación: detecta referencias colgadas entre familias sin FK dura), FASE 5 (matching de
  `cromo_pelos.servicio_numero` contra `app.servicios`, método `REGEX_EXACTO` únicamente por ahora).
  Clasificación CREADA/ACTUALIZADA/SIN_CAMBIOS sólo para botella y cable (los únicos con `vmax` propio);
  tubo/pelo/fusión se upsertean sin clasificación individual, siguiendo el diagrama de
  `docs/Doc Privada/ingesta_cromo.md` §7.1. Tolerancia a errores por objeto vía `session.begin_nested()`
  (savepoint): un objeto roto no aborta el resto de la página.
- `db/models/cromo.py`: `tipo_asociacion` ahora declara `schema="app"` explícito en el `SQLEnum` (ver
  hallazgo below).
- `tests/test_cromo_ingesta.py` (nuevo, 18 tests): clasificación por `vmax`, tolerancia a errores,
  fases con cliente/páginas fake, reconciliación, matching de servicios. 93% de cobertura en
  `ingesta.py`.

### Hallazgos reales durante la validación (los tres bloquearon la primera corrida real)

1. **Cable embebido en `botella.tp[]` sin `id`/`vmax`.** No es una vista con versión distinta: a veces
   directamente no trae esos campos (es una referencia de topología enriquecida, no una foto completa
   del objeto). Clasificar por `vmax is None` explícito: si la fila ya existe se trata como
   SIN_CAMBIOS (nunca pisa datos completos con una vista parcial); si no existe, se omite (no se puede
   insertar con `version_id`/`vmax` `NOT NULL`) — Fase 2, que corre primero y sin este límite en
   producción, la cubre de todas formas.
2. **`asyncpg` no resuelve el enum `cromo_tipo_asociacion_pelo` sin schema explícito.** El
   `search_path` de la conexión es `"$user", public` (no incluye `app`), y el dialecto asyncpg de
   SQLAlchemy renderiza el cast del bind parameter sin calificar por esquema salvo que el `SQLEnum` del
   modelo declare `schema="app"` explícitamente — a diferencia de `CamaraEstado`, que nunca lo necesitó
   porque nada lo insertaba todavía vía el motor async. Es la primera escritura async con un enum
   propio de este esquema en el repo.
3. **FK entre módulos no se resuelve si sólo se importa `db.models.cromo`.** `CromoServicioMatch.servicio_id`
   referencia `app.servicios.id` (modelo `Servicio` en `db/models/infra.py`) por nombre de tabla; si
   `infra.py` nunca se importó, SQLAlchemy no puede resolver la FK al hacer flush. Fix autocontenido:
   `ingesta.py` importa `Servicio` explícitamente, sin depender de que la app lo haya cargado por otro
   lado.

También se agregó `await sesion.rollback()` en el manejador de excepción de tope de `ejecutar_ingesta`:
sin eso, una falla no capturada por los savepoints deja la sesión en `PendingRollbackError` y ni
siquiera se puede cerrar la corrida como `FALLIDA`.

### Etapa 4 — API y progreso en vivo

- `core/services/cromo/ingesta.py`: split de `ejecutar_ingesta()` en `iniciar_corrida()` (ya existía) +
  `continuar_corrida(cliente, sesion, corrida_id, ...)` (nueva) — necesario para que el endpoint
  devuelva el `corrida_id` antes de que las fases corran. Cancelación cooperativa entre páginas
  (`_fue_cancelada_externamente`, consulta fresca — el objeto en memoria no ve un `UPDATE` de otra
  sesión) que corta con `_CorridaCancelada` y cierra como `CANCELADA`, no `FALLIDA`. Eventos nuevos
  `INICIO`/`FASE`/`PAGINA`/`RESUMEN` en `cromo_ingesta_eventos` (con el payload SSE en `detalle` como
  JSON) — el vocabulario de eventos SSE del diseño (§9.1) no coincide con el de auditoría por objeto.
- `web/app/main.py`: `POST /api/admin/ingesta/cromo` (dispara, devuelve `corrida_id`, corre el resto en
  `asyncio.create_task`), `GET .../corridas/{id}/stream` (SSE, traduce `cromo_ingesta_eventos` a
  `inicio`/`fase`/`pagina`/`error`/`resumen`/heartbeat, replay por `Last-Event-ID`), `GET .../corridas`
  (histórico paginado), `GET .../corridas/{id}` (detalle + eventos), `POST .../cancelar`. Sigue el
  patrón vigente del archivo: `_require_admin`, CSRF inline contra `request.session`, imports locales
  de `core.services.cromo.*`/`db.*` dentro de cada función.
- `tests/test_web_cromo_ingesta.py` (nuevo, 13 tests): auth/CSRF/validación, disparo con background
  mockeado, cancelar, histórico, detalle, stream. `tests/test_cromo_ingesta.py` ampliado con tests de
  cancelación y `continuar_corrida` (22 tests en total ahí).

### Etapa 5 — Interfaz

- `web/frontend/src/api/cromo.ts` (nuevo): cliente API del SPA sobre `request`/`requestJson` de
  `src/api/client.ts` (`iniciarIngestaCromo`, `cancelarIngestaCromo`, `obtenerHistoricoCromo`,
  `obtenerDetalleCromo`, `streamUrlIngestaCromo`) + catálogo estático de clases botella (mismo seed que
  la migración de Etapa 2, para no depender de una llamada extra sólo para poblar el selector).
- `web/frontend/src/admin/views/AdminIngestaCromo.vue` (nuevo): selector de clases, `psize` como
  conjunto fijo de 5 valores (radio/select, no input libre), card de progreso en vivo consumiendo el
  SSE de la Etapa 4 con `EventSource` nativo del browser (primer uso en el proyecto — no existe
  composable `useEventSource`, se manejó inline por no haber un segundo consumidor que justifique la
  abstracción), histórico con click-to-detail. `onMounted` reconecta automáticamente si ya hay una
  corrida `EN_CURSO` al entrar a la vista.
- `web/frontend/src/router/index.ts`: ruta `ingesta/cromo` (`meta: { requiresAdmin: true }`).
  `web/frontend/src/admin/views/AdminIngesta.vue`: tercera tarjeta del hub, apuntando a la vista nueva.
  Se siguió la skill `frontend-spa-architecture` (entry point único, router unificado, sin vistas
  huérfanas).
- `core/services/cromo/config.py`: fix en `enmascarar()` — ver hallazgo abajo.
- `tests/test_cromo_client.py`: nuevo test (`test_enmascarar_acota_asteriscos_en_tokens_muy_largos`)
  para el fix de arriba.

**Hallazgo real durante el smoke-test con un token de Cromo real**: `enmascarar()` (usada en los logs
de `client.py::_obtener_token`) enmascaraba 1:1 con la longitud real del secreto. Un JWT de OAuth2 real
de Cromo tiene ~2000 caracteres, así que el log de "token obtenido" quedaba con ~2000 asteriscos en una
sola línea — inutilizable para lectura humana. Fix: tope de `max_asteriscos=20`, sin cambiar la firma
pública ni el comportamiento para secretos cortos (contraseñas, client secrets) ya cubiertos por los
tests existentes.

### Etapa 6 — Verificador de servicios

- `core/services/cromo/verificador.py` (nuevo): `servicios_por_cable`, `servicios_por_tubo`,
  `servicios_por_botella` — SQL crudo con `text()` (mismo estilo que `ingesta.py::fase_servicios`),
  join `cromo_pelos` → `cromo_servicio_match` → `app.servicios`, filtrado por `cable_n_id`/`tubo_n_id`
  directo, o por cables cuyo `extremo_a_n_id`/`extremo_b_n_id` es la botella consultada. No sigue
  fusiones (alcance recortado deliberadamente frente al diseño §8.2 — detalle en `docs/Doc
  Privada/ingesta_cromo.md` §13.3, no pedido por el roadmap público).
- `web/app/main.py`: `GET /api/infra/cromo/{cables,tubos,botellas}/{n_id}/servicios` — `_require_auth`,
  no `_require_admin` (consulta de sólo lectura sobre datos ya ingeridos, no administración; mismo
  criterio que el resto de `/api/infra/*`). 404 vía `ObjetoNoEncontrado` si el `n_id` no existe en
  ningún lado.
- `web/frontend/src/api/cromo.ts`: `verificarServiciosPor{Cable,Tubo,Botella}` + interfaces TypeScript.
  `web/frontend/src/views/VerificadorCromoView.vue` (nuevo): selector de tipo de objeto, input de
  `n_id`, tabla de servicios encontrados. Vista del panel operativo (`/infra/cromo/verificador`), no de
  `/admin` — cualquier usuario autenticado puede consultarla.
- `web/frontend/src/router/index.ts`: ruta nueva como hija de `/` (no de `/admin`).
  `web/frontend/src/components/app-shell/AppShell.vue`: entrada "Verificador Cromo" bajo el grupo
  "Tool Kit" del sidebar (`sidebarModules`, `moduleByView`, `resolveCurrentView`) — se siguió el patrón
  que realmente renderiza el menú, no los campos `navLabel`/`navSection`/`navOrder` presentes en el
  meta de varias rutas de `router/index.ts`, que resultaron estar muertos (ningún componente los lee).
- `tests/test_cromo_verificador.py` (nuevo, 12 tests), `tests/test_web_cromo_verificador.py` (nuevo,
  8 tests): incluyen los tres casos de referencia colgada (cable/tubo/botella con servicio pero sin
  fila propia) encontrados al validar contra datos reales.

**Hallazgo real durante la validación contra `lasfocasdev-postgres`**: dos cables (`n_id` 10191706 y
10191707) ingeridos en la Etapa 3 tienen pelos con servicio matcheado (`ESPN SUR SRL`, servicio
114699) pero **sin fila propia en `cromo_cables`** — la misma "referencia colgada" que audita la fase
de reconciliación (`REF_COLGADA`): el tubo bajó dentro del árbol de una botella en la Fase 3 antes de
que la Fase 2 barriera la página que trae el cable como objeto de primera clase. El criterio inicial de
"no encontrado" (basado sólo en la fila propia) devolvía 404 sobre un cable con servicios realmente
activos — el peor falso negativo posible para esta herramienta. Fix: "no encontrado" ahora es "sin fila
propia **y** sin nada que lo referencie" (ningún pelo con ese `cable_n_id`/`tubo_n_id`, ningún cable con
esa botella como extremo). Cuando falta la fila propia, sólo la metadata (nombre, capacidad, clase)
queda en `null` — los servicios encontrados no se ven afectados.

## Comandos Ejecutados

- `pytest -q` → `358 passed, 5 skipped` (suite completa, sin regresiones).
- `pytest tests/test_cromo_ingesta.py tests/test_cromo_client.py tests/test_cromo_parser.py --cov=core.services.cromo` → 93% en `ingesta.py`, 93% total del paquete.
- `ruff check core/services/cromo/ tests/test_cromo_ingesta.py db/models/cromo.py` → sin hallazgos.
- Corrida real (`ejecutar_ingesta`, `psize=5`, `max_paginas=1`) contra Cromo de producción y
  `lasfocasdev-postgres`: primera corrida con errores (los 3 hallazgos arriba), corridas siguientes
  `estado=OK`, `errores=0`, `sin_cambios=10` en la repetición (confirma idempotencia real, no sólo en
  mocks). `refs_colgadas=148` en esta corrida acotada es esperable: con `max_paginas=1` en ambas fases
  independientes, la mayoría de padres/extremos referenciados todavía no bajaron — no es un defecto.
- `pytest -q` (tras Etapa 4) → `375 passed, 5 skipped`. `ruff check web/app/main.py` → mismos 14
  hallazgos preexistentes que antes de tocar el archivo (verificado por diff de conteo), ninguno nuevo.
- `TestClient` completo contra `web.app.main.app` real (no un mock del archivo): login real vía
  `/api/auth/login`, sesión/cookie reales, `_require_admin`/CSRF reales. Sólo se mockean las funciones
  de servicio (`iniciar_corrida`, la tarea de background) y `AsyncSessionLocal`. No se probó contra un
  browser real con `EventSource` (pendiente para Etapa 5) ni contra el contenedor `lasfocasdev-web`
  corriendo (necesitaría rebuild de imagen; se evaluó y se decidió no hacerlo esta sesión).
- **Etapa 5** — `pytest -q` → `376 passed, 5 skipped` (suite completa, incluye el test nuevo de
  `enmascarar()`). `ruff check` sobre todos los archivos nuevos/modificados de Etapa 5 → sin hallazgos.
- **Etapa 5** — validación de punta a punta sin browser real (Playwright bloqueado por falta de
  `libnspr4.so` del sistema, sin permisos de instalación; `chromium-cli` tampoco disponible en este
  entorno): se levantó `uvicorn` local contra `lasfocasdev-postgres` y el Cromo real, se creó un usuario
  admin temporal (`_cromo_smoketest`, borrado al final), y se verificó a mano con HTTP/SSE crudo el
  contrato exacto que consume `AdminIngestaCromo.vue` — login real, `POST /api/admin/ingesta/cromo`
  (`psize=5`), lectura del stream SSE hasta `resumen` (corrida real: 27 leídas, 0 creadas, 10 sin
  cambios, 0 errores, 148 refs colgadas), `GET .../corridas` y `GET .../corridas/{id}`. Cada campo que
  el TypeScript de `cromo.ts` espera se confirmó presente con el tipo correcto. La corrida de prueba
  (id=5) y el usuario temporal se borraron de `lasfocasdev-postgres` al terminar.
- **Etapa 6** — `pytest -q` → `396 passed, 5 skipped`. `ruff check core/services/cromo/verificador.py
  tests/test_cromo_verificador.py tests/test_web_cromo_verificador.py web/app/main.py` → sin
  hallazgos nuevos (los 14 preexistentes de `main.py` no cambiaron). `vue-tsc --noEmit` y `vite build`
  sobre el frontend → sin errores en los archivos nuevos (los 4 errores preexistentes de `InfraTab.vue`
  no relacionados, sin cambios).
- **Etapa 6** — validación contra `lasfocasdev-postgres` real con los datos ya ingeridos en la Etapa 3
  (usuario temporal `_cromo_verif_smoketest`, rol `user`, borrado al terminar): login sin rol admin,
  `GET .../cables/10191706/servicios` y `GET .../tubos/10191747/servicios` devuelven el servicio real
  de ESPN SUR SRL con metadata en `null` (referencia colgada real, ver hallazgo arriba);
  `GET .../botellas/10191705/servicios` (botella real sin servicios matcheados) devuelve `servicios: []`;
  `GET .../botellas/6633045/servicios` (botella conocida sólo por ser extremo de un cable, sin fila
  propia) devuelve 200 con metadata en `null`; `GET .../cables/999999999/servicios` devuelve 404; sin
  cookie de sesión, 401. Nota metodológica: `docker exec psql` usa auth local de confianza dentro del
  contenedor (ignora `PGPASSWORD`) — la contraseña real para conexión TCP externa está en el secreto
  Docker `.secrets/Dev_db_password_v1.txt`, no en `POSTGRES_PASSWORD` de `.env.dev` (placeholder).

## Criterios de Aceptación

- [x] Transacción por página (commit único por página, no por corrida).
- [x] Clasificación CREADA/ACTUALIZADA/SIN_CAMBIOS por `vmax` para botella y cable.
- [x] Un objeto malformado no aborta el resto de la página (savepoints).
- [x] Auditoría de corrida con contadores en vivo (`cromo_ingesta_corridas`/`_eventos`).
- [x] Reconciliación de referencias colgadas entre familias sin FK dura.
- [x] Validado contra Cromo real y Postgres real, no sólo con mocks.
- [ ] Matching de servicios (Fase 5) sólo cubre `REGEX_EXACTO`; `REGEX_PARCIAL`/`MANUAL` quedan sin
      implementar — primera versión conservadora, a validar contra más datos reales.
- [ ] Corrida completa sin `max_paginas` (producción real, ~1.100 páginas): no se ejecutó en esta
      sesión, deliberadamente — es una operación de carga real, no una prueba.
- [x] (Etapa 4) `corrida_id` disponible de inmediato al disparar, corrida corre en background.
- [x] (Etapa 4) SSE con replay por `Last-Event-ID`, heartbeat, corte al terminar la corrida.
- [x] (Etapa 4) Cancelación cooperativa: `POST .../cancelar` + corte entre páginas, cierra `CANCELADA`.
- [x] (Etapa 4) SSE consumido con `EventSource` real (validado en Etapa 5, ver abajo) — sólo faltaba
      un consumidor real, ya no un `TestClient`.
- [x] (Etapa 5) Vista `/admin/ingesta/cromo`: selector de clases, `psize` fijo a 5 valores, progreso en
      vivo, histórico y detalle.
- [x] (Etapa 5) Contrato de datos verificado byte a byte contra el backend real (HTTP/SSE crudo).
- [ ] (Etapa 5) Renderización visual en un browser real: no verificada — este entorno no tiene permisos
      para instalar las dependencias de sistema de Chromium ni `chromium-cli` disponible. Pendiente
      para quien tenga un entorno con esos permisos.
- [ ] (Etapa 5) No se probó con dos pestañas simultáneas contra la misma corrida (debería funcionar,
      cada `EventSource` es una conexión HTTP independiente sobre las mismas filas, pero no se verificó).
- [x] (Etapa 6) Servicios de un cable/tubo/botella resueltos por consulta directa, sin requests a Cromo.
- [x] (Etapa 6) Tolerante a referencias colgadas: un objeto sin fila propia pero referenciado por otro
      no se trata como "no encontrado" (hallazgo real, ver arriba).
- [x] (Etapa 6) Endpoints de sólo lectura sin rol admin (`_require_auth`), consistente con `/api/infra/*`.
- [ ] (Etapa 6) No implementa "impacto de tocar una botella" (fusiones + pelos que unen) del diseño
      §8.2 — recorte deliberado de alcance frente a lo pedido por el roadmap público (ver arriba).
- [ ] (Etapa 6) Renderización visual en un browser real: no verificada, mismo gap de entorno que Etapa 5.

## Impacto y Riesgos

- **Impacto operativo**: ninguno sobre producción de LAS-FOCAS. Todo corrió contra
  `lasfocasdev-postgres` y contra Cromo real en modo sólo lectura (autorización ya cubierta en Etapa 1).
- **Riesgos conocidos**: una corrida real completa sin acotar puede tardar y transferir varios cientos
  de MB (ver estimación de Etapa 1, §12.2 de `docs/Doc Privada/ingesta_cromo.md`); no se corrió esta
  sesión. Una corrida que crashea fuera de cualquier savepoint (p.ej. la app se cae) puede dejar una
  fila en `EN_CURSO` sin cerrar — pasó una vez durante el debugging de esta sesión (corrida descartada
  manualmente); Etapa 4/5 debería considerar detectar corridas `EN_CURSO` sin actividad reciente.
- **Seguridad y datos**: ninguna credencial nueva. Los datos escritos en `lasfocasdev-postgres` durante
  la validación son reales (botellas/cables/tubos/pelos de Cromo) pero de bajo volumen (5 botellas, 5
  cables, ~1.660 pelos) — se dejan como semilla útil para probar Etapas 4-6, no se truncaron.
- **(Etapa 4) Corrida en background sin supervisión del proceso**: si el proceso de `web/app/main.py`
  se reinicia mientras una corrida corre, la tarea de `asyncio.create_task` muere sin cerrar la corrida
  — queda `EN_CURSO` para siempre (mismo riesgo ya señalado en Etapa 3, ahora con más superficie: el
  endpoint la puede disparar cualquier admin). Etapa 5 debería mostrar/filtrar corridas `EN_CURSO`
  sin actividad reciente en la UI.
- **(Etapa 4) SSE sin proxy_buffering configurado**: si en algún momento se interpone un reverse proxy
  productivo, el capítulo 9.1 ya advertía que necesita `proxy_buffering off` sobre esa ruta — no
  aplica en dev (`lasfocasdev-web` se accede directo), pendiente cuando se defina el despliegue de esta
  vista.
- **(Etapa 5) Gap de prueba visual, no de contrato**: la vista no se vio renderizada en un browser real
  durante esta sesión (limitación del entorno, no del código). El riesgo residual es puramente de
  presentación (CSS, disposición, algo que no rompería el contrato HTTP/SSE ya verificado); recomendable
  una pasada visual antes de dar por cerrada la Etapa 5 de cara a un usuario real.
- **(Etapa 6) Sin control de volumen de resultados**: las tres consultas no tienen `LIMIT` — un cable
  o botella con cientos de servicios devolvería todos en una sola respuesta. No es un riesgo hoy (el
  volumen real observado es bajo, servicios de a pocos por cable) pero si el patrón cambia conviene
  paginar; se dejó sin paginar deliberadamente por ser la primera versión y no haber evidencia de que
  haga falta.
- **(Etapa 6) Alcance recortado, ya señalado**: no sigue fusiones ("impacto de tocar una botella"). Si
  se termina necesitando, es una cuarta función en el mismo módulo, no un rediseño.

## Compatibilidad y Migraciones

- **DB/Alembic**: sin migración nueva. El único cambio de esquema es en el modelo ORM
  (`SQLEnum(..., schema="app", ...)`), no en la tabla física — la migración `20260805_01` ya creaba el
  tipo correctamente calificado; el modelo sólo no lo reflejaba para el caso de escritura async.
- **Dependencias**: ninguna nueva (usa `asyncpg`, ya presente en `requirements.txt`, y `AsyncSessionLocal`
  de `db/session.py`, ya existente).
- **Breaking changes**: ninguno. Primer código que escribe en `app.cromo_*` (Etapa 3); primer uso de
  `AsyncSessionLocal`/tarea en background/SSE en `web/app/main.py` (Etapa 4) — ningún endpoint
  existente cambia de comportamiento.

## Validación Manual

Ver "Comandos Ejecutados". Corrida real repetida dos veces tras las correcciones: segunda corrida
`estado=OK`, `sin_cambios=10`, `errores=0` — confirma que reingestar los mismos datos no duplica ni
falla. Etapas 4, 5 y 6 validadas de punta a punta contra el backend real (Cromo +
`lasfocasdev-postgres`) por HTTP/SSE crudo, con `EventSource` real siendo el consumidor final del
contrato de la Etapa 5 (a través del código de la vista, aunque sin verse renderizado en pantalla), y
con un usuario sin rol admin confirmando que el verificador de la Etapa 6 no requiere permisos
administrativos. Usuarios temporales y corrida de prueba borrados al terminar.

## Próximos Pasos

- Roadmap del módulo completo (Etapas 1 a 6). Los pendientes que quedan son de pulido, no de alcance:
- Pasada visual de `AdminIngestaCromo.vue` y `VerificadorCromoView.vue` en un browser real cuando haya
  un entorno con permisos para instalar las dependencias de sistema de Chromium (o `chromium-cli`
  disponible).
- Decidir si limpiar o conservar los datos de semilla dejados en `lasfocasdev-postgres` por esta sesión.
- Considerar detectar corridas `EN_CURSO` sin actividad reciente (proceso caído a mitad de corrida).
- Si hace falta, extender el verificador con "impacto de tocar una botella" (fusiones + pelos que
  unen) — alcance recortado deliberadamente en la Etapa 6, ver arriba.
- `REGEX_PARCIAL`/`MANUAL` del matching de servicios (Fase 5) siguen sin implementar — sólo
  `REGEX_EXACTO`, señalado desde la Etapa 3.

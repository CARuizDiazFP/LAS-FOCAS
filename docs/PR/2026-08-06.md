# Nombre de archivo: 2026-08-06.md
# Ubicación de archivo: docs/PR/2026-08-06.md
# Descripción: PR diario del 2026-08-06 — Etapa 3 de ingesta Cromo (servicio de ingesta real)

# PR Diario - 2026-08-06

## Resumen de Cambios

Etapa 3 del módulo de ingesta Cromo: servicio real (`core/services/cromo/ingesta.py`) que lee paginado
desde Cromo, clasifica por `vmax`, escribe en `app.cromo_*` con transacción por página, y audita cada
corrida. Validado con una corrida real acotada (`max_paginas=1`) contra el Cromo de producción de
Metrotel y `lasfocasdev-postgres`, que encontró y permitió corregir tres bugs reales antes de comitear.

## Contexto y Alcance

- **Módulos afectados**: `core/services/cromo/ingesta.py` (nuevo), `db/models/cromo.py` (fix puntual de
  esquema del enum).
- **Objetivo**: cerrar la Etapa 3 del roadmap (`docs/modulo_ingesta_cromo.md`), habilitada tras la Etapa
  2 (persistencia) y las decisiones de producto del usuario sobre la clase 69 (ODF) y `psize`.

## Cambios Realizados

- `core/services/cromo/ingesta.py`: FASE 1 (conteo vía `stats[].count`), FASE 2 (cables, barrido
  directo), FASE 3 (botellas + árbol completo: fusiones, cables embebidos, tubos, pelos), FASE 4
  (reconciliación: detecta referencias colgadas entre familias sin FK dura), FASE 5 (matching de
  `cromo_pelos.servicio_numero` contra `app.servicios`, método `REGEX_EXACTO` únicamente por ahora).
  Clasificación CREADA/ACTUALIZADA/SIN_CAMBIOS sólo para botella y cable (los únicos con `vmax` propio);
  tubo/pelo/fusión se upsertean sin clasificación individual, siguiendo el diagrama de
  `docs/Doc Privada/ingesta_cromo.md` §7.1. Tolerancia a errores por objeto vía `session.begin_nested()`
  (savepoint): un objeto roto no aborta el resto de la página.
- `db/models/cromo.py`: `tipo_asociacion` ahora declara `schema="app"` explícito en el `SQLEnum` (ver
  hallazgo below).
- `tests/test_cromo_ingesta.py` (nuevo, 18 tests): clasificación por `vmax`, tolerancia a errores,
  fases con cliente/páginas fake, reconciliación, matching de servicios. 93% de cobertura en
  `ingesta.py`.

### Hallazgos reales durante la validación (los tres bloquearon la primera corrida real)

1. **Cable embebido en `botella.tp[]` sin `id`/`vmax`.** No es una vista con versión distinta: a veces
   directamente no trae esos campos (es una referencia de topología enriquecida, no una foto completa
   del objeto). Clasificar por `vmax is None` explícito: si la fila ya existe se trata como
   SIN_CAMBIOS (nunca pisa datos completos con una vista parcial); si no existe, se omite (no se puede
   insertar con `version_id`/`vmax` `NOT NULL`) — Fase 2, que corre primero y sin este límite en
   producción, la cubre de todas formas.
2. **`asyncpg` no resuelve el enum `cromo_tipo_asociacion_pelo` sin schema explícito.** El
   `search_path` de la conexión es `"$user", public` (no incluye `app`), y el dialecto asyncpg de
   SQLAlchemy renderiza el cast del bind parameter sin calificar por esquema salvo que el `SQLEnum` del
   modelo declare `schema="app"` explícitamente — a diferencia de `CamaraEstado`, que nunca lo necesitó
   porque nada lo insertaba todavía vía el motor async. Es la primera escritura async con un enum
   propio de este esquema en el repo.
3. **FK entre módulos no se resuelve si sólo se importa `db.models.cromo`.** `CromoServicioMatch.servicio_id`
   referencia `app.servicios.id` (modelo `Servicio` en `db/models/infra.py`) por nombre de tabla; si
   `infra.py` nunca se importó, SQLAlchemy no puede resolver la FK al hacer flush. Fix autocontenido:
   `ingesta.py` importa `Servicio` explícitamente, sin depender de que la app lo haya cargado por otro
   lado.

También se agregó `await sesion.rollback()` en el manejador de excepción de tope de `ejecutar_ingesta`:
sin eso, una falla no capturada por los savepoints deja la sesión en `PendingRollbackError` y ni
siquiera se puede cerrar la corrida como `FALLIDA`.

## Comandos Ejecutados

- `pytest -q` → `358 passed, 5 skipped` (suite completa, sin regresiones).
- `pytest tests/test_cromo_ingesta.py tests/test_cromo_client.py tests/test_cromo_parser.py --cov=core.services.cromo` → 93% en `ingesta.py`, 93% total del paquete.
- `ruff check core/services/cromo/ tests/test_cromo_ingesta.py db/models/cromo.py` → sin hallazgos.
- Corrida real (`ejecutar_ingesta`, `psize=5`, `max_paginas=1`) contra Cromo de producción y
  `lasfocasdev-postgres`: primera corrida con errores (los 3 hallazgos arriba), corridas siguientes
  `estado=OK`, `errores=0`, `sin_cambios=10` en la repetición (confirma idempotencia real, no sólo en
  mocks). `refs_colgadas=148` en esta corrida acotada es esperable: con `max_paginas=1` en ambas fases
  independientes, la mayoría de padres/extremos referenciados todavía no bajaron — no es un defecto.

## Criterios de Aceptación

- [x] Transacción por página (commit único por página, no por corrida).
- [x] Clasificación CREADA/ACTUALIZADA/SIN_CAMBIOS por `vmax` para botella y cable.
- [x] Un objeto malformado no aborta el resto de la página (savepoints).
- [x] Auditoría de corrida con contadores en vivo (`cromo_ingesta_corridas`/`_eventos`).
- [x] Reconciliación de referencias colgadas entre familias sin FK dura.
- [x] Validado contra Cromo real y Postgres real, no sólo con mocks.
- [ ] Matching de servicios (Fase 5) sólo cubre `REGEX_EXACTO`; `REGEX_PARCIAL`/`MANUAL` quedan sin
      implementar — primera versión conservadora, a validar contra más datos reales.
- [ ] Corrida completa sin `max_paginas` (producción real, ~1.100 páginas): no se ejecutó en esta
      sesión, deliberadamente — es una operación de carga real, no una prueba.

## Impacto y Riesgos

- **Impacto operativo**: ninguno sobre producción de LAS-FOCAS. Todo corrió contra
  `lasfocasdev-postgres` y contra Cromo real en modo sólo lectura (autorización ya cubierta en Etapa 1).
- **Riesgos conocidos**: una corrida real completa sin acotar puede tardar y transferir varios cientos
  de MB (ver estimación de Etapa 1, §12.2 de `docs/Doc Privada/ingesta_cromo.md`); no se corrió esta
  sesión. Una corrida que crashea fuera de cualquier savepoint (p.ej. la app se cae) puede dejar una
  fila en `EN_CURSO` sin cerrar — pasó una vez durante el debugging de esta sesión (corrida descartada
  manualmente); Etapa 4/5 debería considerar detectar corridas `EN_CURSO` sin actividad reciente.
- **Seguridad y datos**: ninguna credencial nueva. Los datos escritos en `lasfocasdev-postgres` durante
  la validación son reales (botellas/cables/tubos/pelos de Cromo) pero de bajo volumen (5 botellas, 5
  cables, ~1.660 pelos) — se dejan como semilla útil para probar Etapas 4-6, no se truncaron.

## Compatibilidad y Migraciones

- **DB/Alembic**: sin migración nueva. El único cambio de esquema es en el modelo ORM
  (`SQLEnum(..., schema="app", ...)`), no en la tabla física — la migración `20260805_01` ya creaba el
  tipo correctamente calificado; el modelo sólo no lo reflejaba para el caso de escritura async.
- **Dependencias**: ninguna nueva (usa `asyncpg`, ya presente en `requirements.txt`, y `AsyncSessionLocal`
  de `db/session.py`, ya existente).
- **Breaking changes**: ninguno. Primer código que escribe en `app.cromo_*`; nadie más las usa todavía.

## Validación Manual

Ver "Comandos Ejecutados". Corrida real repetida dos veces tras las correcciones: segunda corrida
`estado=OK`, `sin_cambios=10`, `errores=0` — confirma que reingestar los mismos datos no duplica ni
falla.

## Próximos Pasos

- Etapa 4: endpoints (`POST /api/admin/ingesta/cromo`, SSE de progreso con replay por `Last-Event-ID`
  leyendo `cromo_ingesta_eventos`) y validación de `psize` reutilizando `PSIZE_PERMITIDOS`.
- Etapa 5: vista `/admin/ingesta/cromo` (selector de `psize` fijo a 5 valores, no input libre).
- Etapa 6: verificador de servicios sobre las tablas ya pobladas.
- Decidir si limpiar o conservar los datos de semilla dejados en `lasfocasdev-postgres` por esta sesión.

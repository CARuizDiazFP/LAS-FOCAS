# Nombre de archivo: 2026-09-04.md
# Ubicación de archivo: docs/cierres/2026-09-04.md
# Descripción: Cierre de sesión — 2026-09-04: fix del flujo Ingreso/Baneo vía Slack e investigación del supuesto bug de parseo PROV en servicios BAJA

# Cierre de sesión — 2026-09-04

> **Nota de consolidación (2026-09-10):** este archivo reúne las retrospectivas de **dos sesiones
> independientes** del 2026-09-04 que escribieron cada una su propio cierre para el mismo día en
> ramas separadas. Al unificar las ramas se conservaron ambas íntegras, cada una con su alcance y
> sus límites de evidencia originales.

## Sesión: Fix flujo Ingreso/Baneo vía Slack (rama `fix/ingreso-baneo-slack-tecnico`)

### Alcance y límites de evidencia

Retrospectiva basada en la conversación activa completa de esta sesión (sin compactación intermedia
detectada, salvo un corte real de sesión por rate-limit del modelo `opus` durante la primera revisión
final de rama — la sesión se retomó limpiamente gracias al ledger de `subagent-driven-development`,
que sobrevivió el corte; ningún hecho de esta sección depende de memoria no verificable). Diff, logs de
contenedores, salidas de `pytest` y consultas SQL reales citadas abajo fueron observados directamente
en esta sesión, no inferidos.

### Tareas de la sesión (contrastadas contra evidencia real)

**Ticket original**: 4 bugs reportados en el flujo "ingreso de técnico a cámara vía Slack" — (1) un
intento bloqueado por baneo se registraba en DB como ingreso normal; (2) se guardaba el ID crudo de
Slack en vez del nombre del técnico; (3) sin botella especificada, se mostraba null/"Sin botella
asociada" en vez de "Botella 1"; (4) el badge web y el chequeo de Slack no detectaban baneos manuales
(sin incidente de protección asociado).

**Investigación previa al plan** (Explore + lectura directa de código): corrigió 3 premisas del ticket
antes de escribir una sola línea — el endpoint ya devolvía `tiene_baneo_activo` (no hacía falta un
campo nuevo `has_active_ban`), `core/parsers/tracking_parser.py` no tiene nada que ver con Slack (el
parser real es `camara_search.py`), y "Botella 1" es una convención de etiqueta ya codificada
(`detectar_multi_bot`), no una entidad faltante. El bug real detrás del punto 4 se aisló con precisión:
`get_camara_estado_contexto()` sólo miraba `IncidenteBaneo`, nunca `Camara.estado == BANEADA`.

**Plan** (`superpowers:writing-plans`): 9 tareas, guardado en
`docs/superpowers/plans/2026-09-04-ingreso-baneo-slack-fix.md`.

**Ejecución** (`superpowers:subagent-driven-development`, worktree aislado): las 9 tareas se
implementaron y revisaron (implementador + revisor por tarea), con 2 rondas de fix loop reales:
- Tarea 5 (listener grupo-consciente): el revisor encontró que el test insignia de la tarea
  (`test_baneo_de_botella_hermana_bloquea_ingreso_a_la_camara_raiz`) nunca ejercitaba un grupo
  multi-miembro real (`camara.botellas = []` hardcodeado) — corregido con un test que efectivamente
  construye una botella hermana baneada y verifica tanto el texto de respuesta como el id pasado a
  `obtener_ultimo_motivo_baneo_manual`.
- Tarea 6 (serializador `botella_label`): el revisor encontró que la rama "Botella legado" (cámara con
  `camara_padre_id` propio, sin `CromoBotella` asociada) nunca se testeaba — corregido con un caso real.

**Tarea 9 (verificación E2E real)**: migración `20260904_01` aplicada contra `lasfocasdev-postgres`
real (confirmado vía `\d app.ingresos`); contenedores `lasfocasdev-api`/`web`/`slack-baneo-worker`
reconstruidos desde el worktree y confirmados healthy sin errores de arranque; chequeo directo contra
dato real de dev (cámara 2628: `estado_actual=BANEADA`, `incidentes_activos=[]`,
`tiene_baneo_activo=True` correctamente) — exactamente el escenario que el bug original fallaba.

**Revisión final de rama completa** (modelo `opus`, dos intentos — el primero cortado por rate-limit,
retomado limpio): encontró **1 Critical + 6 Important reales fuera del diff de las 9 tareas** —
exactamente la clase de bug que ninguna revisión acotada por tarea podía ver, porque cada una revisaba
sólo los archivos que su propia tarea tocaba:

| # | Hallazgo | Archivo | Impacto real confirmado |
|---|---|---|---|
| C1 (Critical) | Ampliar `tiene_baneo_activo` (Tarea 3) rompió `baneos_grupos_service.py`, nunca auditado por el plan | `core/services/baneos_grupos_service.py` | 87 grupos baneados reales en dev, 0 incidentes activos → el panel admin de "Baneos activos" bloqueaba el 100% de la liberación masiva sin "forzar" |
| I1 | `protection_service.py::_determinar_estado_restauracion` sin filtro `tipo` | `core/services/protection_service.py:728` | `lift_ban` restauraría permanentemente `OCUPADA` en vez de `LIBRE` si hubo un intento bloqueado |
| I2 | Filas viejas con ID crudo de Slack nunca cerrables por un Egreso post-deploy | `core/services/ingreso_service.py` | 1 fila real stranded en dev (cámara 2628, `tecnico_id='U03DPFK0Q69'`) |
| I3 | `_evaluar_estado_acceso_camara` perdió el guard fail-open de la función eliminada | `modules/slack_baneo_notifier/listener.py` | una excepción de DB dejaría de responder en Slack (peor que el comportamiento previo) |
| I4 | `ServicioDetalleView.vue` comparte serializador pero nunca se actualizó | `web/frontend/src/views/ServicioDetalleView.vue` | el bug original (Intento indistinguible de ingreso real) resurgía en esa pantalla |
| I5 | `botella_label` podía seguir siendo `null` (CromoBotella con `nombre=NULL`) | `web/app/main.py::_serialize_camara_ingreso` | contradice la garantía documentada "nunca null" |
| I6 | `docs/api.md`/`docs/db.md` fuera del alcance de la Tarea 8, seguían describiendo el contrato viejo | — | gap de plan, no de implementación |

**Fix wave único + re-revisión** (per protocolo SDD: una sola ronda de fix + una re-revisión acotada,
sin segunda ronda): los 8 hallazgos (Critical+Important+1 minor atado) se corrigieron en 5 commits
enfocados. La re-revisión verificó cada uno de forma independiente (incluyendo re-correr 207 tests del
alcance tocado y spot-check de 2 de las fallas "ambientales" del full-suite) — **todo direccionado, sin
regresión nueva**. Durante el propio fix wave, el implementador encontró y corrigió una regresión
adicional real (el campo nuevo `tiene_incidente_activo` rompía un fixture de
`tests/test_slack_baneo_worker.py` no mencionado por el plan) — verificado por la re-revisión.

**Suite completa** (corrida por el controlador, no sólo confiada al reporte del subagente): `pytest
tests/ -q` → 1312 passed, 27 failed, 5 skipped, 28 errors. Los 27+28 se rastrearon a exactamente 2 causas
ambientales del worktree bare (sin red Docker: `postgres` no resuelve por DNS; sin build de frontend:
falta `web/frontend/dist/`) — confirmado que ningún archivo fallando aparece en ningún commit de esta
rama.

### Errores/bloqueos reales enfrentados y solución aplicada

**1. Contaminación de rama compartida entre sesiones concurrentes.**
- Síntoma: al arrancar, la rama efímera creada con `dev-workflow` (`checkout -b` normal, sin worktree)
  tenía un commit ajeno encima (`docs(cierres): investigación real descarta bug de parseo PROV en
  BAJA`) que yo no había hecho.
- Causa raíz confirmada: una sesión hermana (`las-focas-86`, verificada con `ListAgents`) operaba en el
  mismo checkout compartido (`/home/support-focal-01/LAS-FOCAS`) y commiteó sobre el `HEAD` activo en
  ese momento, que resultó ser mi rama efímera recién creada — sin aislamiento de directorio real entre
  sesiones, cualquier commit de cualquiera de las dos aterriza en la rama que esté checked-out.
- Solución aplicada: no se borró ni forzó nada. Se avisó a la sesión hermana vía `SendMessage`, se
  abandonó esa rama sin tocarla, y se creó un **worktree nuevo fuera del árbol del repo**
  (`/home/support-focal-01/LAS-FOCAS-worktrees/fix-ingreso-baneo-slack-tecnico`, no bajo `.worktrees/`
  del repo, para evitar depender del `.gitignore` compartido) desde `origin/dev` limpio. La sesión
  hermana movió su propio commit a una rama propia y limpió la rama contaminada por su lado.
- Validación: `git reflog`/`git log` confirmaron el punto exacto de contaminación; `ListAgents` confirmó
  la sesión hermana activa; coordinación bilateral confirmada por mensaje antes de proceder.
- Condición de regresión futura: cualquier sesión larga (SDD, migraciones) en este repo debería preferir
  un worktree desde el inicio si hay sospecha de trabajo concurrente — no hay guardrail automático que
  lo detecte hoy (evolución agéntica aplicada más abajo).

**2. `env_file`/`secrets` relativos rotos al reconstruir Docker desde el worktree.**
- Síntoma esperado y ya documentado en una skill previa (`docker-rebuild/SKILL.md`, sección "Rebuild
  desde un worktree") — confirmado real de nuevo esta sesión: `docker compose up -d` sin symlinks
  falla porque `env_file`/`secrets` del compose son rutas relativas al propio archivo, no al
  `--env-file` del flag.
- Solución aplicada: symlinks temporales `.env.dev`/`.secrets` apuntando al checkout principal,
  confirmados `git check-ignore` antes de crearlos, eliminados al terminar el rebuild. Sin cambios a la
  skill (ya estaba completa y correcta — no hizo falta ampliarla).
- Efecto colateral esperado y confirmado sin pérdida de datos: `postgres` también se recreó (drift de
  `depends_on` preexistente, ya documentado); la migración se aplicó y los datos reales (incluida la
  fila de la cámara 2628) siguieron presentes después.

**3. Rate-limit de sesión del modelo `opus` durante la primera revisión final de rama.**
- Síntoma: el subagente de revisión final terminó abruptamente con "You've hit your session limit"
  a mitad de su análisis.
- Causa: límite de uso de la cuenta para ese modelo específico, no un error del proceso.
- Solución aplicada: el usuario pidió un corte prolijo; se confirmó el estado real (worktree intacto,
  ledger SDD con las 9 tareas ya marcadas completas) antes de retomar, y se re-despachó la revisión
  final exitosamente en el turno siguiente, sin perder ningún hallazgo del trabajo previo (el ledger es
  justamente la garantía de esto).
- Condición de regresión futura: si un despacho de subagente en modelo `opus` termina con "session
  limit"/"rate_limit" a mitad de tarea, no reintentar en el mismo turno — confiar en el ledger SDD para
  retomar en un turno posterior, nunca re-derivar el estado de memoria.

**4. Gaps reales del propio plan, encontrados durante la implementación (no errores del implementador).**
- La Tarea 5 nombraba explícitamente sólo 4 sitios de test a corregir tras eliminar
  `_obtener_incidentes_activos_camara`, pero existían 17 sitios reales que la referenciaban en
  `tests/test_slack_ingreso_listener.py`. El implementador encontró los 13 no listados vía RED (18
  fallos, no los pocos que el brief implicaba) y los corrigió con dos patrones justificados (parche
  muerto eliminado donde `camara=None` hacía el código inalcanzable; parche reemplazado por
  `get_camara_estado_contexto` donde correspondía) — verificado exhaustivamente por el revisor de tarea.
- La revisión final (ver tabla arriba) es la manifestación más grande de este mismo patrón: un cambio
  de semántica de un campo ampliamente compartido (`tiene_baneo_activo`) sólo audita los consumidores
  que el propio plan ya toca, no todos los reales del repo.

### Vigencia de documentación

`docs/bot.md`, `docs/infra.md` (Tarea 8), `docs/api.md`, `docs/db.md` (fix wave, hallazgo I6)
actualizados para reflejar el comportamiento real post-fix — incluyendo el nuevo campo
`tiene_incidente_activo` y el contrato corregido del panel de baneos.

### Evolución agéntica

**Prevención — 🟢 Bajo (agrega guardrail a una skill existente, sin tocar permisos/push/merge):**
cambio de semántica de un campo/función compartida requiere grep-audit de TODOS los consumidores
reales del repo antes de dar la tarea por completa, no sólo los que el plan ya toca. Evidencia: hallazgo
C1 de la revisión final (arriba). Frecuencia esperada: cualquier refactor de una función de estado
ampliamente reusada (patrón ya visto en este repo: `camara_estado_service.py`, `protection_service.py`,
`baneos_grupos_service.py` comparten el mismo dominio). Beneficio: mueve la detección de "revisión final
de rama" (cara, tardía) a "diseño de la tarea" (barata, temprana). Costo de mantenimiento: ninguno, es
una línea de guardrail. **Implementado**: `alembic-migrations/SKILL.md` (fuente +
`.github`/`.codex-skills`/`.gemini` mirrors — no existe mirror en `.claude/skills/` para esta skill,
gap preexistente ya documentado en `CLAUDE.md`).

**Prevención — 🟢 Bajo (amplía guardrail existente de `dev-workflow`, sin mecanismo nuevo):** preferir
un worktree desde el arranque de cualquier tarea larga cuando haya sospecha de sesión concurrente
(verificable con `ListAgents`), no sólo reaccionar después de una colisión. Evidencia: incidente real #1
de arriba. Frecuencia esperada: cualquier sesión de `subagent-driven-development` o trabajo largo en
este repo, dado que es común tener múltiples sesiones Claude Code activas en la misma máquina.
Beneficio: elimina la clase de bug en vez de repararla reactivamente. Costo de mantenimiento: ninguno.
**Implementado**: `dev-workflow/SKILL.md` (fuente + `.claude`/`.github`/`.codex-skills` mirrors —
guardrail 7 ampliado).

No hubo propuestas 🔴 — ninguna toca permisos de `main`/producción ni introduce un mecanismo de
push/merge nuevo. Sin compuerta de riesgo activada, se continuó sin pausas.

### Archivos de gobernanza tocados (paso 12)

- `.agentes-comunes/skills/alembic-migrations/SKILL.md` (fuente) + `.github/skills/alembic-migrations/SKILL.md` + `.codex-skills/skills/las-focas-alembic-migrations/SKILL.md` + `.gemini/rules/skill-alembic-migrations.md` — guardrail nuevo #4 (grep-audit de consumidores en cambios de semántica compartida).
- `.agentes-comunes/skills/dev-workflow/SKILL.md` (fuente) + `.claude/skills/dev-workflow/SKILL.md` + `.github/skills/dev-workflow/SKILL.md` + `.codex-skills/skills/las-focas-dev-workflow/SKILL.md` — guardrail 7 ampliado (preferir worktree ante sospecha de sesión concurrente).

### Flujo recursivo SDD — métricas de ciclo

9 tareas del plan + 1 ronda de fix loop en Tarea 5 (1 Important) + 1 ronda de fix loop en Tarea 6 (1
Important) + 1 revisión final de rama con 1 fix wave único (1 Critical + 6 Important) + 1 re-revisión
acotada (limpia, sin segunda ronda). Ninguna tarea alcanzó el cap de 5 rondas. Ninguna alerta de
`docs/sdd_metricas_ciclo.md` aplicable (sin ciclos repetidos ni hallazgos re-abiertos).

### Estado de la rama al cierre

`fix/ingreso-baneo-slack-tecnico` (forkeada de `origin/dev` @ `8c8d1fc`), 16 commits, worktree en
`/home/support-focal-01/LAS-FOCAS-worktrees/fix-ingreso-baneo-slack-tecnico`. Migración
`20260904_01` ya aplicada contra `lasfocasdev-postgres` real. Contenedores `lasfocasdev-api`/`web`/
`slack-baneo-worker` ya reconstruidos y verificados healthy con el código de esta rama. Lista para
integrar a `dev` — ver flujo de auto-merge de este mismo cierre.

---

## Sesión: Investigación "bug de parseo PROV" en servicios BAJA (sin cambios de código)

### Límites de evidencia declarados

- Sesión operada en **modo Plan** durante casi todo el trabajo (sólo lectura + un plan file); no se
  ejecutó ningún cambio de código, no hubo commits propios de esta sesión.
- Rama activa al iniciar y al cerrar: `fix/ingresos-slack-baneo-flujo` — **preexistente**, no creada
  en esta sesión. Al momento de este cierre, esa rama es idéntica a `origin/dev` (0 commits de
  diferencia en ambos sentidos): no hay nada de esta sesión para mergear.
- Se detectó una **sesión paralela activa** (`las-focas-ef`, peer, iniciada ~1h antes) trabajando
  sobre esa misma rama en una tarea completamente distinta ("Fix flujo Ingreso/Baneo vía Slack" —
  bloqueo de Ingreso, resolución de nombre de técnico, herencia de baneo entre Botellas), evidenciada
  por un archivo sin trackear `docs/superpowers/plans/2026-09-04-ingreso-baneo-slack-fix.md`
  (timestamp 12:09 UTC de hoy) que esta sesión **no creó ni modificó**. Ver "Merge a Dev" abajo.

### Tarea planteada

El usuario reportó (rol asignado: "Ingeniero de Software Backend / Web Scraping / Integración de
APIs") un supuesto bug en `core/services/prov/` (o `core/parsers/`): para servicios PROV en estado
"BAJA" (ejemplos dados: 41146, 100560), la sincronización no persistía Cliente ni Domicilio, mientras
que en "ALTA" (119732) sí funcionaba. Hipótesis del ticket: un "early return"/excepción en el parser
al no encontrar el bloque de "Contexto" ("Contexto no creado" en la UI de PROV) bloqueaba también la
extracción de Cliente/Domicilio, pese a que esos datos aparecen en una "tabla superior de metadatos"
separada en la UI de PROV.

### Investigación realizada y hallazgos

1. **Localización real del módulo** (2 agentes Explore en paralelo): `core/services/prov/` existe
   (`client.py`, `config.py`, `ingesta.py`, `rate_limiter.py`), pero **no es web scraping de HTML** —
   es un cliente REST JSON contra `API_Contexto_Servicio` de PROV
   (`https://prov.metrotel.com.ar/api/v1/ADMEQ/API_Contexto_Servicio`). `core/parsers/` no tiene
   relación (sólo parsers de Excel/tracking). El string literal "Contexto no creado" **no existe en
   ningún lugar del repo**. Documentado ya en `docs/superpowers/specs/2026-09-02-servicios-prov-integracion-design.md`,
   `docs/decisiones.md` y `docs/db.md` desde la integración original (2026-09-02).
2. **Verificación contra el código real**: `parsear_contexto_prov`/`ingerir_contexto_prov`
   (`core/services/prov/ingesta.py`) usan guards (`if parseado.nombre_cliente:`, `if
   parseado.equipos:`) que ya evitan pisar datos existentes con `None`, y
   `consolidar_identidad_servicio` ya descarta valores vacíos (`_es_valor_util`) sin corromper la
   identidad del servicio. `ProvServicioNoEncontradoError` (la única excepción real del cliente) sólo
   se dispara cuando `Resultado:` es un string (servicio inexistente en PROV) — no tiene relación con
   estados ALTA/BAJA.
3. **Verificación contra el sistema real** (crítico — la premisa del ticket no se dio por válida sin
   probarla): se consultó `ProvClient` real, con las credenciales ya configuradas en
   `.secrets/Dev_api_prov_*`, contra los 3 números de servicio exactos del ticket, desde
   `lasfocasdev-api`:
   - **119732 (INSTALADO)**: `Resultado:` completo (`Descripcion`, `Direccion1`, `Provincia1`,
     `Nodo1`, `Equipo1`, `Port1`, etc.) — como se esperaba.
   - **41146 y 100560 (DADO BAJA)**: `Resultado:` **es un objeto exitoso** (no dispara ninguna
     excepción) pero PROV lo devuelve truncado a
     `{"subproducto": ..., "nro_servicio_original": ..., "estado_comercial": "DADO BAJA"}` — sin
     `Descripcion` ni `Direccion{N}`/`Provincia{N}` en absoluto.
   - **Conclusión verificada**: la premisa del ticket (early-return/excepción de nuestro código) es
     **incorrecta**. El código de LAS-FOCAS no bloquea ni pierde datos disponibles; el gap es que
     **PROV mismo no incluye Cliente/Domicilio en este endpoint para servicios de baja**.
4. **Descarte de endpoint alternativo**: se buscó en todo el repo cualquier otra referencia `API_*`
   de PROV (sin resultados) y se probó `GET .../API_ADMEQ/help` contra el sistema real para ver si
   la cuenta tenía visibilidad de otras acciones — devolvió `401: "api-claude no esta autorizado a
   utilizar la accion ADMEQ:help"`. Esto confirma que PROV autoriza por acción de forma granular y
   que la cuenta `api-claude` está limitada a `Contexto_Servicio`. Se decidió **no** seguir probando
   nombres de acción a ciegas contra el sistema de producción de Metrotel (enumeración no autorizada).

### Clasificación de la tarea

**No verificada como bug de LAS-FOCAS — descartada tras evidencia real.** No se trata de una tarea
"completada" ni "bloqueada" en el sentido de una implementación pendiente: es un hallazgo de que el
bug reportado no existe en el código propio; la causa real está fuera del repo (dato no expuesto por
PROV + alcance de autorización de la cuenta de servicio).

### Decisión y próximos pasos (con el usuario)

Se presentó el hallazgo completo al usuario (incluyendo el detalle de autorización por-acción) antes
de escribir ningún plan de implementación, dado que cambiaba por completo el alcance del pedido
original. El usuario decidió: **va a consultar con quien administra los permisos de la cuenta
`api-claude` en PROV** si existe otra acción con Cliente/Domicilio para servicios de baja y si puede
autorizarse — y cerrar esta sesión sin tocar código hasta tener esa respuesta.

Un plan de ejecución quedó redactado (no aplicado) en
`/home/support-focal-01/.claude/plans/rol-eres-un-ingeniero-sprightly-hopcroft.md` para cuando se
retome, con alcance ya acotado: (1) logging de observabilidad en `ingerir_contexto_prov` cuando un
`Servicio` queda sin `nombre_cliente`/`direccion` tras un refresh PROV, (2) tests con la forma real
del payload truncado de BAJA, (3) addendum en `docs/decisiones.md` y en el spec de diseño de PROV
documentando este hallazgo. Explícitamente fuera de alcance: no se puede fabricar Cliente/Domicilio
que PROV no envía, y no se prueban más acciones de PROV sin autorización confirmada.

### Evolución agéntica

Ninguna propuesta esta sesión — no hubo obstáculo repetible de tooling/skills que ameritara una skill
o guardrail nuevo. El propio proceso de "no dar por válida la premisa del ticket sin probarla contra
el sistema real" ya está cubierto por memoria de sesiones previas (no requiere una skill nueva).

### Actualización de documentación

Ninguna actualización de `docs/decisiones.md` ni del spec de PROV en esta sesión — quedaron
propuestas en el plan pendiente de ejecución (ver arriba), no implementadas, porque el usuario cerró
antes de aprobar el plan. Este archivo de cierre es la única documentación nueva de la sesión.

### Merge a Dev

**Diferido — no ejecutado, y no por clasificación de riesgo 🔴.** Motivos concretos:

1. Esta sesión no generó ningún commit: la rama `fix/ingresos-slack-baneo-flujo` ya es idéntica a
   `origin/dev` (0 commits de diferencia) — no hay nada propio de esta sesión para integrar.
2. Se detectó una **sesión paralela activa** (`las-focas-ef`) usando esa misma rama ahora mismo para
   una tarea distinta y aún no comiteada (plan `docs/superpowers/plans/2026-09-04-ingreso-baneo-slack-fix.md`,
   sin trackear, de hace minutos). Ejecutar el flujo de auto-merge de este skill —que termina
   borrando la rama local y remota (`git branch -d` + `git push origin --delete`)— habría destruido
   la rama que esa sesión está usando activamente, con su trabajo aún sin commitear. Se prioriza no
   tomar una acción destructiva sobre un proceso concurrente detectado en vivo, sobre completar el
   checklist mecánicamente.

No se ejecutó ningún comando de git más allá de lectura (`status`, `log`, `branch --show-current`)
sobre esta rama.

---

## Checklist final

- Análisis retrospectivo completado
- Evolución agéntica: sin propuestas esta sesión
- Actualización de documentación: sólo este reporte de cierre (cambios de docs de PROV quedaron en el plan pendiente, no ejecutados)
- Merge a Dev: **diferido** — nada propio para mergear + rama compartida con una sesión paralela activa (no se borra la rama)
- Final de sesión
